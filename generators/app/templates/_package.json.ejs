{
  "name": "<%= name %>",
  "private": true,
  "dependencies": {
    <%- Object.keys(dependencies).map(function(name) {
      return '"'+name+'": "'+dependencies[name]+'"';
    }).join(',\n    '); %>
  }
}

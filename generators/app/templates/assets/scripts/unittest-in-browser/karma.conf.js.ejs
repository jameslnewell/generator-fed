module.exports = function(config) {
  config.set({

    basePath: '<%= scriptDir %>',

    browserify: {
      debug: true
    },

    client: {
      mocha: {
        reporter: 'html'
      }
    },

    files: [

      //polyfill .bind() for phantom JS <v2.0
      process.cwd() + '/node_modules/phantomjs-polyfill/bind-polyfill.js',

      //test files
      'test/**/*.{js,jsx}'

    ],

    frameworks: ['mocha', 'browserify', 'source-map-support'],

    preprocessors: {
      'test/**/*.{js,jsx}': ['browserify']
    },

    reporters: ['dots']

  });
};

{
  "name": "<%= name %>",
  "private": true,
  "devDependencies": {
    <%- Object.keys(dependencies).sort().map(function(name) {
      return '"'+name+'": "'+dependencies[name]+'"';
    }).join(',\n    '); %>
  },
  "browserify": {
    "transform": <%- JSON.stringify(transforms); %>
  }
}
